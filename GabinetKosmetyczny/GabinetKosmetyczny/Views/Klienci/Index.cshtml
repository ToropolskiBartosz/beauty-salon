@model IEnumerable<GabinetKosmetyczny.klienci>

@{
    ViewBag.Title = "Index";
}

<h2>Klienci</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.imie)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.nazwisko)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.telefon)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.email)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.plec)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.imie)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.nazwisko)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.telefon)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.email)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.plec)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_klienta }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_klienta }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_klienta })
        </td>
    </tr>
}

</table>
